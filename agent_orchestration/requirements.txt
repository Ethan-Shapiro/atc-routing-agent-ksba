# Phase 3 (LangGraph orchestration)
#
# langgraph/langchain-anthropic are left unpinned so pip resolves a mutually-compatible
# langchain-core for both, rather than risking the same silent-downgrade issue hit with
# Airflow's requirements.txt (see data_pipeline/requirements.txt's comment) by pinning an
# old langchain-core alongside them.
langgraph
langchain-anthropic
# Pinned to match mcp_server/requirements.txt's version for SDK behavior parity between
# this MCP client and the server it talks to.
mcp==1.2.1
fastapi==0.115.8
uvicorn==0.34.0
asyncpg==0.30.0
pydantic-settings
python-dotenv==1.0.1
